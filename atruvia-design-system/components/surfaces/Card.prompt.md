Rounded content container; use for grouping content, teasers, feature tiles.

```jsx
<Card raised media={<img src="…" />}>
  <h3>Girokonto</h3>
  <p>…</p>
</Card>
```

Props: `raised` (shadow instead of border), `interactive` (hover lift + pointer),
`media` (flush top image area).
